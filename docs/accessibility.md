# Accessibility Notes

The portfolio uses semantic HTML elements, descriptive image alternative text, labeled form fields, and keyboard-friendly links and controls.

The JavaScript also respects the user's `prefers-reduced-motion` setting by skipping animated entrance effects when reduced motion is requested.

When updating the site, preserve meaningful heading order, visible focus states, readable contrast, and useful labels for interactive elements.
